services:
  casabella:
    image: casabella-api
    container_name: casabella-api

    build: .

    ports:
      - "5000:5000"

    volumes:
      - .:/app
      - /app/node_modules

    env_file:
      - ./config/.env

    command: npm run dev